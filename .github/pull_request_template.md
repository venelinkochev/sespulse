## What this changes

<!-- 1–3 sentences on the user-visible change -->

## Why

<!-- linked issue, motivation, or "fixes #123" -->

## Testing

- [ ] `docker compose up -d --build` succeeds from a clean checkout
- [ ] `npm run build` succeeds
- [ ] Verified the change locally (describe how)

## Screenshots

<!-- For UI changes, include before/after. Dark mode only is fine — it's the default. -->
